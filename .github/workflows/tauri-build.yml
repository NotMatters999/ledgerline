name: Tauri Build

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
          cache-dependency-path: 'pnpm-lock.yaml'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: 'src-tauri -> target'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Tauri App
        run: pnpm run tauri build

      # Bundled installers: MSI (Windows Installer) + NSIS (lightweight setup exe)
      - name: Upload Installers
        uses: actions/upload-artifact@v4
        with:
          name: ledgerline-windows-installers
          path: |
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/bundle/nsis/*.exe

      # Portable build: raw unsigned exe — no installation required, run directly.
      # Tauri v2 has no dedicated 'portable' bundle target; this IS the portable build.
      - name: Upload Portable Exe
        uses: actions/upload-artifact@v4
        with:
          name: ledgerline-windows-portable
          path: src-tauri/target/release/ledgerline.exe
